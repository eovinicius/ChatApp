name: Terraform Plan

on:
  pull_request:
    branches:
      - main
    paths:
      - "infra/**"
  workflow_dispatch:
    inputs:
      environment:
        description: "Ambiente para rodar o plan manualmente"
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
        default: prod

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  # prod roda o plan automaticamente em todo PR que toque infra/**,
  # e também via "Run workflow" escolhendo prod.
  plan-prod:
    if: >
      github.event_name == 'pull_request' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'prod')
    uses: ./.github/workflows/reusable/terraform.yml
    with:
      environment: production
      label: prod
      working-directory: infra/environments/prod
      action: plan
    secrets:
      aws-role-arn: ${{ secrets.AWS_ROLE_ARN_PROD }}
      aws-region: ${{ secrets.AWS_REGION }}
      jwt-secret-key: ${{ secrets.TF_VAR_JWT_SECRET_KEY }}

  # dev/staging só rodam plan manualmente via "Run workflow".
  plan-dev:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'dev'
    uses: ./.github/workflows/reusable/terraform.yml
    with:
      environment: development
      label: dev
      working-directory: infra/environments/dev
      action: plan
    secrets:
      aws-role-arn: ${{ secrets.AWS_ROLE_ARN_DEV }}
      aws-region: ${{ secrets.AWS_REGION }}
      jwt-secret-key: ${{ secrets.TF_VAR_JWT_SECRET_KEY }}

  plan-staging:
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging'
    uses: ./.github/workflows/reusable/terraform.yml
    with:
      environment: staging
      label: staging
      working-directory: infra/environments/staging
      action: plan
    secrets:
      aws-role-arn: ${{ secrets.AWS_ROLE_ARN_STAGING }}
      aws-region: ${{ secrets.AWS_REGION }}
      jwt-secret-key: ${{ secrets.TF_VAR_JWT_SECRET_KEY }}
